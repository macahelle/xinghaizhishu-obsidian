# Tmac 五项整改验收记录

- 日期：2026-08-15
- 主题：3.2.0
- 插件：1.2.4
- 桌面环境：Obsidian 1.13.7 / macOS

## 验收结论

1. 未重做或替换既定主题外观；修改仅限项目入口、专注内部几何、日历排列和反向链接移除。
2. “进行中的项目”右上角已提供新增入口；Tmac 中实际打开表单，自动测试实际调用创建逻辑并校验 `type: project`、`status: active`、项目目标与初始任务。为避免污染正式库，Tmac 表单未提交。
3. 专注标题、目标、倒计时、进度条和按钮采用不收缩纵向布局，底部按钮与卡片边框不再重叠。
4. 2026 年 8 月日历采用 macOS 日历一致的周日起始，固定显示 42 格，范围为 2026-07-26 至 2026-09-05。
5. 原生反向链接叶片已移除，未迁移到底部；状态栏也不显示反向链接统计。右栏只保留星海信息栏。

## 可复核证据

- `before.jpeg`：整改前界面。
- `final-workbench.jpeg`：整改后完整工作台，包含完整日历、专注卡片和无反向链接右栏。
- `project-modal.jpeg`：Tmac 中实际打开的新增项目表单。
- `../doc-quicklook-1.2.4/`：更新后的安装指导与产品手册 macOS 原生缩略图。

## 自动验证

- `node --check xinghai-workbench/main.js`：通过。
- `node tests/plugin-core.test.js`：通过，包含真实项目 Markdown 创建断言。
- `node tests/validate-package.js`：通过，包含工作区语义和 21 文件发布白名单。
- `unzip -t release/星海知枢-工作台-v3.2.0-正式版.zip`：通过。
- ZIP SHA-256：`73a6ca54f310fef05c282d8d9082ab4fc016204947fef431fb2783c059b2701f`。

## 限制

- 按用户口径，本版移动端只做开发适配、窄容器模拟与自动检测，未做真实手机 Obsidian 验收。
