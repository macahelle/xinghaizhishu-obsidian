# 星海知枢工作台主题包安装说明

## 完整安装

> 3.2.0 的移动端根据代码适配、窄容器模拟和自动检测发布，未做真实手机 Obsidian 触控验收。在手机上使用前建议先备份知识库并用副本试用。

安装工作区预设前请先完全退出 Obsidian，避免应用退出时用旧布局覆盖新文件。

假设知识库名为 `Tmac`：

1. 将 `星海知枢` 整个文件夹放入：
   `Tmac/.obsidian/themes/星海知枢`
2. 将 `xinghai-workbench` 整个文件夹放入：
   `Tmac/.obsidian/plugins/xinghai-workbench`
3. 打开 Obsidian，在“设置 → 外观 → 主题”选择“星海知枢”。
4. 进入“设置 → 第三方插件”，开启第三方插件并启用“星海知枢工作台”。
5. 点击左侧 Ribbon 的星轨图标，或从命令面板运行“打开星海知枢工作台”。插件会创建中央工作台和右侧信息栏。

`workspace-xinghai.json` 和 `graph-xinghai.json` 仅作为可选预设；先备份原文件，再在完全退出 Obsidian 后替换。插件本身不要求覆盖现有工作区。

重新打开后应看到：

- 左侧为原有知识库文件树；
- 中央上方为根据一级目录动态生成、可点击的知识星图；
- 中央下方为四模块工作台；
- 右侧为日历、今日时间线、关联笔记和标签；
- 三栏、星图和四模块在一个屏幕内显示，不互相叠压。

## 深色与浅色

进入“设置 → 外观 → 基础颜色方案”：

- 深色：紫蓝星海、深色玻璃卡片；
- 浅色：雾蓝星云、白色高可读卡片。

两种外观由同一个主题提供，无需重复安装。

## 日常使用

- 点击“新增任务”打开分类记录窗口；内容为空时创建按钮保持禁用，目录或时间格式错误会在窗口内提示并把焦点移回对应字段；
- 点击“进行中的项目”右上角 `+` 新增项目；首行填写项目名、其余内容填写项目目标，并由用户明确选择保存目录；
- 点击“本周复盘”进入复盘沉淀；
- “今日三件事”可直接勾选并保存；保存期间会显示操作中状态，失败时会恢复原状态并提示；
- 点击已有日记的日期会直接打开；点击没有日记的日期会先询问，确认后才创建 Markdown 文件；
- 已有项目、目录和当前专注均可点击进入真实笔记；
- “最近笔记”自动更新，无需手工维护。
- 窄屏使用顶部“星图 / 工作台 / 日历 / 关联”标签切换；若桌面侧栏把中央区域挤得过窄，可点击“收起侧栏”，收起后使用工作台左上角的“恢复侧栏”还原收起前状态。

## 回滚

1. 完全退出 Obsidian。
2. 将安装前备份的 `workspace.json` 和 `graph.json` 复制回 `.obsidian/`。
3. 打开 Obsidian，在“设置 → 外观 → 主题”重新选择原主题。

工作台数据保存在普通 Markdown 和插件 `data.json` 中。停用主题不会删除笔记；停用插件后动态工作台会停止显示。
