---
type: log
status: active
updated: 2026-08-15
---
# 下一步任务

## 当前发布任务

- [x] 删除工作台插件、工作区预设、测试知识库和旧工作台发布包。
- [x] 清理 `theme.css` 中工作台选择器、运行时变量和固定侧栏宽度。
- [x] 将 README、INSTALL、DOCX 和项目规范改为纯主题口径。
- [x] 用 8 文件白名单重建 `v3.2.0` 主题 ZIP。
- [x] 通过主题包结构与 ZIP 完整性检查。
- [ ] 提交当前纯主题项目并推送 GitHub `main`。
- [ ] 将远程 `v3.2.0` 标签校正到纯主题提交。

## 非发布项

- `qa/` 与 `assets/reference/` 是开发证据，不进入安装包；是否从 Git 仓库永久删除需用户单独明确授权。
- 旧工作台内容仍可能存在于 Git 历史对象中；清理历史需要单独确认后执行历史重写。
- 移动端只做 CSS 适配和模拟检测，不安排真实移动端验收。
