---
type: log
status: active
updated: 2026-08-15
---
# 全局问题与防复发规则

| 日期 | 问题 | 原因 | 防复发规则 | 状态 |
|---|---|---|---|---|
| 2026-07-30 | 把工作台做成单篇 HTML 页面 | 只复刻画面，没有使用真实 Obsidian 数据 | 工作台必须使用插件视图和 Markdown 数据 | 已记录 |
| 2026-07-31 | 把工作台需求缩减为主题背景和普通 Callout | 错误理解“主题不是页面”，忽略完整功能 | 交付前逐项核对效果图中的所有可见功能 | 已记录 |
| 2026-08-01 | 未先确认动态功能的数据来源 | 主题、插件和 Markdown 的职责没有提前冻结 | 开发前确认星图、任务、项目、计时和日历的数据模型 | 已修正 |
| 2026-08-01 | Logo 首版偏传统书本插画感 | 书页造型和复杂知识网络削弱了科技品牌感 | 后续品牌图形优先使用数据星核、电路、互联节点与简洁几何结构 | 已修正 |
| 2026-08-01 | Logo 科技版仍包含中文名称 | 用户希望标志像成熟科技品牌一样可脱离文字独立识别 | Logo 主标志只保留原创几何图形，默认不嵌入汉字或英文 | 已修正 |
| 2026-08-01 | 前三版 Logo 缺少成熟品牌感和核心元素表达 | 未先明确公开软件场景、品牌气质和元素优先级 | 先完成品牌调研；后续固定以未来、专业、探索和宇宙星海为设计准则 | 已修正 |
| 2026-08-01 | V4 Logo 被用户评价“丑爆了” | 粗轨道、廉价霓虹渐变和割裂星体形成通用功能图标感 | 禁止继续修补 V4；品牌稿优先使用扁平矢量、项目既有星轨语义和小尺寸验证 | 已修正 |
| 2026-08-01 | 为模仿参考图使用跨栏浮层，导致图层叠压且无法同屏 | 把视觉叠放当成产品布局，忽略 Obsidian 原生分栏和窗口约束 | 整块工作台必须采用正常网格流并限制在中央叶片；每次验收同时检查左栏、星图、四模块、右栏和标签 | 已修正 |
| 2026-08-01 | 单屏整改后工作台视觉密度与参考图仍有明显差距 | 取消跨栏后没有同步重算中央有效宽度、字号、行高和右栏图谱密度 | 每轮布局改变后必须重新做同视口叠加检查，先保证比例和阅读层级，再调整装饰 | 已修正 |
| 2026-08-01 | 用户视角测试发现日历浏览、任务反馈和专注卡片边缘体验不足 | 首版优先完成数据链路，未覆盖空日期误创建、异步写回反馈和紧凑窗口底部留白 | 交互验收必须检查浏览是否产生写入、异步操作是否有即时反馈、1229 × 768 下文字是否完整可见 | 已修正 |
| 2026-08-01 | 中心星球和翻页时钟被做成近似 CSS 外观，仍与红圈参考明显不同 | 把品牌锚点当成可压缩装饰，未按圆形星球和双翻页卡拆分结构；最终 QA 只看整体同屏 | 参考图红圈或主视觉组件必须单独建立尺寸与 DOM 结构验收项，局部并排未通过不得宣告整体 QA 通过 | 已修正 |
| 2026-08-01 | 二级页面缺少清晰的一键返回主页入口 | 只依赖文件打开和标签页切换，没有检查从一级工作台进入二级内容后的导航闭环；测试库还隐藏了 Ribbon | 所有一级入口必须验证“进入二级页 → 全局主页入口可见 → 一键返回原工作台”；工作区预设必须启用 Ribbon | 已修正 |
| 2026-08-01 | 把工作台分类与 `00/10/20` 路径绑定，无法迁移到其他人的知识库 | 把当前测试库目录当成产品固定结构，没有建立安装期关系发现和用户确认流程 | 所有写入目标必须来自全库扫描与用户映射；缺少映射时禁止静默回退到硬编码目录 | 已修正 |
| 2026-08-01 | Logo 重绘改变原图案风格，星河与左上星球被近似处理 | 没把用户标注的背景和 Logo 当成不可改造的视觉锚点 | Logo 直接使用参考图原图案资产；星河保持原资产比例，红圈锚点必须做同视口对照 | 已修正 |
| 2026-08-01 | 新增内容使用窄单行输入，无法承载任务说明、灵感正文和文章摘要 | 只按“标题”字段设计，没有按多类知识采集场景检查真实输入长度 | 新增内容默认使用整行多行输入；首行映射标题，其余内容完整写入 Markdown 正文 | 已修正 |
| 2026-08-01 | 浅色主题星球仍沿用深色材质，Logo 因混合模式在白底消失 | 只完成了全局浅色背景，没有逐项检查星球材质和位图在浅色背景上的合成结果 | 每个视觉锚点必须分别验证深浅主题；Logo 必须使用透明底原图案并检查显影、光学中心和高分辨率边缘 | 已修正 |
| 2026-08-02 | 一级目录虽来自知识库但仍被固定数量、坐标和连线模式限制 | 只替换了节点文字数据，没有把布局算法和连线关系改为运行时生成 | 星图必须使用全部一级目录并测试目录数量增减；任何固定节点上限、固定坐标表或固定连线下标都视为数据写死 | 已修正 |
| 2026-08-02 | 四宫格边界挤压、底部缺少全库统计、浅色时钟冒号对比不足 | 紧凑视口规则被后置最小高度覆盖，且只检查模块主体没有检查原生底部状态栏和主题细节 | 桌面 QA 必须同时检查四宫格边界/最后一行内容、状态栏真实数据和深浅主题中间态颜色 | 已修正 |
| 2026-08-02 | Logo 元素盒居中但透明图案的可见边界仍低于标题顶部 | 只按图片元素盒对齐，没有检查透明像素后的实际可见轮廓 | 透明 Logo 必须以可见像素边界做光学对齐；局部 QA 同时检查元素盒和实际轮廓顶部 | 已修正 |
| 2026-08-02 | 星图背景上下出现水平分层 | 宽图使用 `100% auto` 后高度不足，容器空带露出底层背景 | 装饰背景必须同时核对资产与容器宽高比；不允许因按单边缩放露出底层，桌面深浅主题均需做同坐标全高检查 | 已修正 |
| 2026-08-02 | 同级主操作按钮边框一致但内部底色仍分叉 | “本周复盘”遗留 `is-secondary` 青色背景覆盖，只统一了边框，没有检查完整视觉状态 | 用户要求同款按钮时必须共用同一基础选择器，并同时核对默认、悬停、深色和浅色状态，不保留无意义的局部覆盖 | 已修正 |
| 2026-08-02 | 暗面星球被绑定到第一个一级目录，星链采用机械直虚线 | 把装饰星体塞入动态目录节点模型，并沿用最简单的中心直连绘制 | 装饰宇宙元素必须与知识库数据节点解耦；星链连接需按自然轨迹绘制并分别验证深浅主题 | 已修正 |
| 2026-08-15 | 当前源码与 V3.1 发布包严重漂移，包结构检查仍误判通过 | 验证只检查文件存在与 JSON/CSS 结构，没有比较源码、测试库副本、发布目录和 ZIP 的哈希与资产清单 | 每次发布必须执行四方哈希与资产清单一致性检查；任一漂移直接阻断发布 | 已修正 |
| 2026-08-15 | 窄屏标签模式启动后左右侧栏仍挤压中央工作台 | 只按整窗媒体查询切换布局，没有把中央视图实际可用宽度纳入判断 | 窄屏 QA 必须在左右栏开/关、桌面窄窗和真实移动端分别验证，无截断、无横向滚动后才可通过 | 桌面已修正，移动待验 |
| 2026-08-15 | 表单与选择控件缺少完整错误恢复和无障碍状态 | 首版依赖 Notice 与视觉类名，没有同步设计字段级错误、错误聚焦、aria 状态和语义标题 | 所有表单和分段/标签/日历控件必须同时验证鼠标、键盘、可访问性树和错误恢复 | 已修正 |
| 2026-08-15 | ZIP 逐文件验签读取大图时触发 `ENOBUFS` | Node 子进程默认输出缓冲不足以容纳星图 PNG | 读取发布包二进制资产时显式设置 64MB `maxBuffer`，并继续以 SHA-256 比较而非跳过大文件 | 已修正 |
| 2026-08-15 | 候选 ZIP 含 `__MACOSX` 和 `._*` 元数据 | 使用 `ditto --sequesterRsrc` 把资源分叉一并封装 | 发布脚本固定使用 `zip -r -X`，验证脚本禁止 macOS 元数据条目 | 已修正 |
| 2026-08-15 | 一键收起侧栏后没有可见恢复入口 | 只验证了收起动作与中央区域变宽，没有把逆向导航纳入交互闭环 | 所有改变布局的快捷操作必须提供常驻可见的逆向入口，并恢复操作前的精确状态 | 已修正 |
| 2026-08-15 | 全局“星海知枢”与 Logo 看似居中但不在同一视觉水平线 | 按透明图片外框对齐，未复核 Logo 实际可见轮廓与文字基线 | 品牌栏必须同时检查元素盒、透明像素可见边界和文字基线，深浅主题均需局部截图复核 | 已修正 |
| 2026-08-15 | 使用 Python 检查 Logo 透明边界时报缺少 PIL | 未先核对本地可用图像依赖 | 优先使用现有 `view_image`、系统图像工具或已加载的工作区依赖；不得假设 PIL 已安装 | 已记录 |
| 2026-08-15 | 仅凭元素盒与整屏截图误判全局 Logo/标题已经水平对齐 | 没有放大检查最终渲染的发光轮廓与文字字面中心，且过早将 QA 标记通过 | 品牌对齐必须保存局部放大图并以实际可见像素重心复核；用户截图仍显示偏差时立即撤销通过结论 | 已修正 |
| 2026-08-15 | 局部放大后仍凭肉眼第二次误判 Logo/标题对齐 | 没有计算实际像素边界中心，视觉判断被图标光晕和文字字面高度干扰 | Logo 对齐验收必须输出图标与文字各自的像素边界、边界中心和加权重心；数值未收敛不得标记通过 | 已修正 |
| 2026-08-15 | 移动端验收启动后才确认本地测试库未同步到手机 | 发布门禁设置了真实移动端验收，但未提前冻结测试库的设备传输方式 | 移动验收前先确认真机连接、Obsidian 安装、测试库可见性和插件/主题文件同步；若用户取消实机门禁，必须在发布材料中明确标注未实机验证 | 已按用户新口径更新 |
| 2026-08-15 | 新增移动标签断言首次失败 | 测试正则误假设标签是二维双字段数组，实际为 `id/icon/label` 三字段数组 | 新增源码结构断言前先读取实际 DOM/数据定义，不根据界面文案推测内部结构 | 已修正并回归通过 |
| 2026-08-15 | DOCX 首次渲染时中文全部显示为方框 | 捆绑 LibreOffice 无界面版本无法读取 macOS 系统中文字体；设置字体名、字体缓存与 `SAL_FONTPATH` 均未解决 | macOS 中文 DOCX 最终验收改用 Pages/Word 原生引擎导出 PDF，再逐页检查；LibreOffice 方框结果不得作为文档内容缺字结论 | 已修正 |
| 2026-08-15 | 产品手册窄屏截图后的未验证提示被挤到单独页，并出现空白页 | 大图、提示表格后的尾随空段落与下一章显式分页叠加 | 图文密集页必须检查章节尾部；提示位于章末时取消尾随空段落，并重新导出核对总页数与空白页 | 已修正 |

## 长期要求

- 不用静态文字冒充动态统计。
- 不用假复选框或假按钮冒充可操作控件。
- 不擅自改变用户确认的效果图布局和视觉方向。
- 不用 portal、fixed 或 absolute 跨栏摆放整块工作台面板。
- 桌面验收必须以“所有主要图层在同一屏幕可见”为硬性标准。
- 正式知识库安装前必须先提供测试库可视化结果。
- 日历浏览不应在无明确提示时创建新文件；所有写回操作必须提供即时状态和失败提示。
- 一级工作台打开的二级内容必须始终保留左侧 Ribbon 主页入口，不依赖浏览历史按钮。
