---
type: log
status: active
updated: 2026-08-15
---
# 重要决策

## D-001 历史决策：只发布 Obsidian 主题（已废止）

- 用户明确本次发布产品是 Obsidian 主题，不是工作台或社区插件。
- 该边界已被用户后续明确要求废止，现行规则见 D-006。

## D-002 主题与插件职责分离

- 主题不创建项目、任务、专注、日历或时间线，不包含 JavaScript。
- `xinghai-workbench` 插件负责工作台功能、本地内容读取和用户触发的 Markdown 写入。
- 两者同包交付，但安装目录和运行职责保持分离。

## D-003 历史决策：八文件白名单（已废止）

- 已由 D-006 的 19 文件白名单替代。

## D-004 跨平台交付方式

- macOS 与 Windows 使用同一个 ZIP，不生成 `.dmg`、`.pkg`、`.exe` 或 `.msi`。
- 主题安装到 `.obsidian/themes/星海知枢`，插件安装到 `.obsidian/plugins/xinghai-workbench`，均不进入 Obsidian 应用程序目录。

## D-005 GitHub 更新原则

- GitHub 默认分支应同时包含主题 3.2.0 与工作台插件 1.2.6 源码。
- 版本标签和 Release 附件必须与同提交生成的整合 ZIP 一致。
- Git 历史中的旧对象不等于当前版本内容；如需清除全部历史，必须单独执行受控的历史重写。

## D-006 主题与工作台整合发布

- 用户明确要求将 `xinghai-workbench` 1.2.6 纳入 ZIP 和 Git 仓库，替代此前“纯主题发布”边界。
- 发布包使用 19 文件精确白名单：4 个根文档、4 个主题文件、11 个插件文件。
- `data.json`、`.DS_Store`、测试库、QA、参考图、脚本和项目记忆不得进入安装包。

## D-007 ZIP 中文路径兼容

- 发布 ZIP 使用 `bsdtar` 生成，中文路径必须写入 ZIP UTF-8 文件名标记。
- 不再使用当前 macOS `zip` 命令生成正式包，因为其归档缺少 UTF-8 标记，可能导致 Windows 解压中文目录乱码。
- 发布校验必须同时检查 19 文件白名单、逐文件哈希、压缩数据完整性和非 ASCII 路径的 UTF-8 标记。
