---
type: log
status: active
updated: 2026-08-01
---
# 重要决策

## D-015 正式包内置跨平台安装与产品手册

- macOS 与 Windows 共用同一主题/插件 ZIP，不生成伪装成独立应用的 `.dmg/.pkg/.exe/.msi`。
- 正式包根目录内置《macOS 与 Windows 安装指导说明》和《产品操作手册》，并纳入逐文件 SHA-256 一致性验证。
- Windows 指南必须明确知识库内 `.obsidian` 路径、隐藏项目显示、无需管理员权限、不要复制到程序目录或 `%APPDATA%`，以及路径过长的处理方式。
- 文档必须标注移动端只完成代码适配、窄容器模拟和自动检测，未做真实手机触控回归。

## D-014 移动端发布改用代码适配与模拟检测

- 用户于 2026-08-15 明确放弃真实移动端实机验收，不再将真机截图和触控回归作为 3.2.0 发布门禁。
- 移动端质量依据改为：代码响应式分支、中央容器窄屏模拟、无横向溢出、移动标签与可访问性语义、自动回归检查。
- README、安装说明和 QA 状态必须注明“未做真实手机验证”，不得将模拟结果描述为实机通过。

## D-001 采用主题、插件、工作区和 Markdown 四层架构

- 主题负责全局深浅视觉。
- 插件负责动态组件和交互。
- 工作区预设负责 Obsidian 面板布局。
- Markdown/Properties 保存用户数据。
- 原因：主题 CSS 无法创建时钟、任务写回、计时器、日历或动态统计。

## D-002 中央星图采用固定目录导航

- 星图以 `00-Wiki入口` 为中心，连接一级目录和辅助资料目录。
- 节点点击后打开对应索引或目录入口。
- 不用全库随机力导向图代替效果图中的稳定导航结构。

## D-003 项目进度自动计算

- 只读取 `type: project` 且 `status: active` 的笔记。
- 进度等于已完成任务数除以全部任务数。
- 无任务项目显示 0%，不伪造进度。

## D-004 正式安装前必须经过测试库验收

- 所有开发先进入 `test-vault`。
- 深色、浅色、桌面和移动端通过后，再向用户展示并等待安装确认。

## D-005 品牌 Logo 采用公开软件品牌标准

- 使用场景覆盖团队知识库、公开发行软件、官网、应用图标和品牌头像。
- 品牌气质固定为未来、专业、探索；核心视觉聚焦宇宙星海。
- 主标志采用纯图形，不嵌入汉字或英文；必须支持深色、浅色和单色应用。
- V4“星际跃迁之门”因轮廓笨重、渐变廉价和元素割裂被用户否决。
- V5 回到项目原有“开放星轨 + 中央知识入口”语义，以扁平矢量、单一探索坐标和中央负空间重构，不再使用立体高光和随机位图造型。

## D-006 桌面工作台采用单屏原生分栏

- 左侧文件树、中央工作台、右侧信息栏分别留在 Obsidian 原生分栏内。
- 中央采用“星图 46% + 工作台 54%”两行网格；四模块采用 2 × 2 网格。
- 禁止通过 portal、fixed 或 absolute 放置整块工作台；绝对定位仅用于星图节点、连线、局部光效和进度条。
- 原因：参考图中的装饰性跨栏叠放在真实 Obsidian 窗口中会遮挡文件树、造成裁切，并破坏单屏可用性。

## D-007 浏览与写回操作必须显式反馈

- 已有日记日期可以直接打开；没有日记的日期必须先确认，禁止因浏览日期静默创建 Markdown 文件。
- 任务写回期间必须禁用重复操作并显示保存中状态；成功给出提示，失败恢复原状态并显示错误。
- 紧凑窗口中的动态统计应放在稳定的标题区，不依赖容易被裁切的卡片底部空间。

## D-008 采用组件级质量门禁和阻断式发布

- 参考图中的视觉锚点必须按照真实结构实现，不能用单一容器、近似渐变或压缩变形代替。
- 每个视觉锚点独立建立参考局部图、可测量标准、实现截图和并排验收图；前一项未通过，不进入后一项。
- 任何 P0/P1/P2 都会阻断最终 QA 和发布包；测试通过不等于视觉通过，整体同屏不等于局部忠实。
- 只有重新完成局部、全屏、交互、深浅主题和打包验证，才能恢复可交付状态。

## D-009 二级页面使用全局 Ribbon 主页入口返回

- 主页入口放在用户红圈指定的左侧 Ribbon 主操作区末位，不占用一级工作台或二级 Markdown 正文空间。
- 图标语义固定为 `home`，提示固定为“返回星海知枢主页”。
- 点击后复用并显示已有 `xinghai-workbench-view`，不创建重复工作台，不强制关闭当前二级笔记。
- 工作区外观预设必须显示 Ribbon；否则入口虽然已注册但不可见。

## D-010 内容写入采用可迁移的知识库关系映射

- 插件不得把 `00 / 10 / 20` 目录写死为保存目标；这些名称只可能是当前库扫描得到的结果。
- 安装后扫描文件夹、文件名、Properties、标签和已解析链接，为任务、灵感、文章、知识和复盘生成目录建议，由用户确认后持久化。
- 任务、灵感和外部文章是必需映射；用户可暂时忽略首次提示，但新建未映射类型时必须显式选择实际目录。
- 每条记录写入独立 Markdown 和 Properties，并关联目标目录中的入口/索引笔记；工作任务可选择同步到今日三件事。
- 新增内容表单使用多行正文输入，首个非空行生成标题和文件名，其余内容保留在正文，避免长内容被压缩成窄单行字段。

## D-011 视觉锚点必须提供独立深浅主题材质

- 暗面星球、中心主星球和品牌 Logo 不能只依赖全局主题背景自动适配，必须分别定义浅色与深色显影效果。
- Logo 继续使用参考图原四向星核、开放轨道和三节点图案；位图底色改为透明，允许按主题调整对比度，但禁止改变图案结构。
- Logo 的几何位置保持在标题左侧，使用固定图标栏完成水平、垂直和光学中心校准，避免动态标题或断点导致漂移。

## D-012 星链与底部汇总由全库数据实时生成

- 星链以自动识别的入口目录为中心，所有知识库一级目录都进入布局；1–9 个外围目录使用单环，10 个以上使用双环，禁止固定节点数量、目录名称、坐标表和连线下标。
- 一级目录创建或删除后由 Vault 事件触发工作台刷新；密集布局使用向内标签、最大宽度和省略号控制碰撞。
- 底部汇总使用 Obsidian 原生状态栏，只展示不同 Properties 字段数、词数和非空白字符数；按用户要求不显示或迁移反向链接统计。
- 全库文本统计以文件路径和修改时间缓存；只有新增或变更文件重新读取，避免每次刷新重复扫描所有正文。

## D-013 响应式按中央容器宽度触发，发布执行四方一致性门禁

- 当工作台中央容器小于 520px 时，无论应用整窗宽度如何，都切换为“星图 / 工作台 / 日历 / 关联”标签式单列布局。
- 极窄状态提供一键收起左右侧栏，但不自动改变用户工作区；收起前持久化左右栏各自状态，并用独立常驻入口只恢复原本展开的侧栏，禁止形成单向操作死路。星图节点在容器内钳制，避免标签跨栏。
- 全局品牌栏按 Logo 实际可见轮廓而非透明图片外框做光学对齐，标题使用固定 24px 对齐盒，原 Logo 图案资产不变。
- 每个发布候选必须比较源插件/主题、测试库副本、候选目录和 ZIP 内关键文件的内容哈希及资产清单；旧发布包不能作为验证来源。

## D-014 正式 Tmac 按最终项目标准安装

- 用户于 2026-08-15 明确授权把正式版安装到当前 Tmac，解除此前“用户确认前不得安装”的限制；“安装当前项目主题”应按项目完整视觉架构执行，不得擅自缩减为只复制主题和插件。
- 标准外观固定为左侧原生文件树、中央 `xinghai-workbench-view`、右侧全高 `xinghai-workbench-sidebar`，并应用正式外观与图谱配置；不保留会挤压右栏高度的独立反向链接分栏。
- 首次内容目录使用插件扫描建议并由界面确认持久化；不把测试库路径或旧 Markdown 示例任务写入正式库。
- 包内工作区预设与最终插件架构不一致时，必须先报告并修订发布包，禁止把过期预设称为最终标准，也禁止自行改用其他安装口径。
- 安装完成必须通过完全退出后的真实 Obsidian 重载、三栏界面检查、配置解析和安装文件哈希一致性检查。

## D-015 产品 ZIP 采用运行时精确白名单

- Git 开发仓库保留插件源码、自动测试和测试库用于复现与验证，但 `tests/`、`test-vault/` 不属于用户安装包。
- ZIP 只包含主题/插件实际引用的运行文件、3 个可选工作区预设、README、INSTALL 和两份 DOCX。
- 旧式 `00-星海工作台.md`、`03-工作台视图.base`、内部 QA 状态和未被代码引用的图片不得进入正式 ZIP。
- 包验证必须比较精确文件清单；多文件或少文件均阻断发布。

## D-016 本轮界面行为按用户明确口径冻结

- “进行中的项目”必须提供真实新增入口并创建 `type: project`、`status: active` 的 Markdown；测试必须覆盖实际写入内容，不能只检查解析器。
- 日历按 macOS 日历的周日起始与固定六周网格展示，确保月初/月末日期完整。
- 反向链接窗口彻底移除，不迁移到底部或其他位置；工作区右栏只保留星海信息栏。
- 仅修复专注卡片内部几何与溢出，不改变既定主题视觉方向。
