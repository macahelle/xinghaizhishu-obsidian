---
type: rule
status: active
updated: 2026-08-01
---
# 项目协作规则

## 项目目标

开发“星海知枢”Obsidian 工作台，视觉与功能以 `assets/reference/obsidian-workbench-target-v2.png` 为唯一基准。

## 固定架构

- 主题层：深色星海、浅色星雾及 Obsidian 全局组件外观。
- 插件层：星图、时钟、统计、项目、任务、专注、日历、时间线、关联笔记和标签。
- 工作区层：原生左侧文件树、中央工作台、右侧信息面板。
- 数据层：任务、项目和复盘写回 Markdown/Properties；不使用 HTML 假任务或 CSS 假数据。

## 开发约束

- 先在独立测试库验收，用户确认前不安装到 Tmac。
- 保留 `00 / 10 / 20 / 30 / 80 / 90 / Clippings / outputs` 架构。
- 项目进度按项目笔记中的任务完成比例自动计算。
- 今日三件事由用户手动选择，写入当日日记。
- 专注计时支持 25 和 50 分钟。
- 日历只读取 Obsidian 日记和任务，不接入外部日历。
- 桌面和移动端功能完整；移动端允许按标签页重排。用户于 2026-08-15 明确取消真实移动端实机验收，改为代码适配、窄容器模拟和自动检测；发布说明必须显式标注未做真机验证。
- 主题和插件不得依赖未声明的第三方社区插件。

## 验证要求

- 验证插件加载、视图注册、任务写回、项目进度、计时持久化和工作区恢复。
- 深色、浅色分别使用真实 Obsidian 截图与参考图进行设计 QA；移动端使用窄容器模拟截图与自动检测，不宣称实机通过。
- 用户确认前不得把测试版本覆盖到正式知识库。
- 每次任务结束更新 `PROGRESS.md`、`NEXT_TASKS.md` 和必要的决策、问题记录。

## 强制质量门禁

- 参考图是视觉唯一事实源；不得用“相似即可”、临时占位、单容器近似或未经确认的自创样式替代参考图中的核心组件。
- 开始实现前，必须先拆分并测量目标组件的结构、尺寸、比例、层级、间距、颜色、阴影、图像资产、响应式行为和交互状态。
- 品牌 Logo、中心星球、翻页时钟、主要按钮、核心模块及用户红圈标注内容一律视为视觉锚点；视觉锚点不得因紧凑布局被改变结构或降级成近似效果。
- 严格按单项顺序执行：保存参考局部图 → 记录可测量标准 → 实现一个组件 → 同视口截图 → 与参考局部并排检查 → 自检通过 → 再进入下一项。
- 每项自检必须同时检查 DOM/组件结构、几何比例、字体、间距、颜色、光影、资源质量、交互反馈、深浅主题和 1229 × 768 边界。
- 发现任何 P0、P1 或 P2 差异时，当前设计 QA 必须保持 `blocked`；不得宣称“完成”“通过”“可交付”，不得生成推荐安装包。
- 修复后的版本必须重新执行局部对照、全屏对照、核心交互测试、语法检查、包结构检查和压缩包完整性检查；禁止沿用修复前的截图或 QA 结论。
- 所有验收结论必须附带可复核证据路径；不能从截图确认的交互或错误状态必须明确标记为未验证，禁止推测通过。
- 正式发布需要同时满足：自动测试通过、深浅主题通过、目标视口同屏通过、移动窄容器模拟通过、无 P0/P1/P2、项目记忆已更新、用户确认视觉方向。
